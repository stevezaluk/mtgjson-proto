syntax = "proto3";

package booster;

option go_package = "github.com/stevezaluk/mtgjson-models/booster";

message BoosterSheet {
    bool allowDuplicates = 1;
    bool balanceColors = 2;
    map<string, int64> cards = 3;
    bool foil = 4;
    bool fixed = 5;
    int64 totalWeight = 6;
}